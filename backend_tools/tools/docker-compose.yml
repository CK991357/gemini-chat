# docker-compose.yml (最终的、绝对路径绑定版本)

services:
  python-sandbox:
    build: .
    container_name: python-sandbox-service
    ports:
      - "127.0.0.1:8828:8828"
    volumes:
      # 挂载 Docker socket，保持不变
      - /var/run/docker.sock:/var/run/docker.sock
      # 🎯 核心修复：将主机的绝对路径 /srv/sandbox_workspaces 挂载到容器内的相同路径
      - /srv/sandbox_workspaces:/srv/sandbox_workspaces
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    user: "root"